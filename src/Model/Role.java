package Model;

// for SQL, should the role in the database as well?
// would it be a datatype instead of enum?
public enum Role
{
    SYSTEM_ADMIN,
    CAFE_OWNER,
    CAFE_MANAGER,
    CAFE_STAFF
}
